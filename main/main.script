function init(self)
		msg.post(".", "acquire_input_focus")
end

function on_message(self, message_id, message, sender)
	if message_id == hash("on_swipe") then
		print('SWIPE:', message.direction)
	end
end
